using 2 kind of implementation to mock create records on targeting database,
1) create a record on parent table
2) create a record on child table contains a reference id linking to parent table
3) and so on ....